<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="#157e00"
                android:centerColor="#157e00"
                android:centerY="1.0"
                android:endColor="#157e00"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="#b40006"
                    android:centerColor="#b40006"
                    android:centerY="1.0"
                    android:endColor="#b40006"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>